server:
  servlet:
    context-path: /comment
spring:
  application:
    name: comment-service
  datasource:
    url: jdbc:mysql://localhost:33061/comments
    username: nqat0919
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

identity-service:
  domain: lb://identity-service